# Seal Debug 纪律（每次讨论到错误，必须记入 DEBUG_LOG）

> 本文件是「错误必须记录」的铁律说明，配合仓库根目录的 `DEBUG_LOG.md` 使用。
> `DEBUG_LOG.md` 是唯一的错误记录载体；本文件解释为什么要记、记什么、怎么记。

## 一、铁律

**无论哪一次讨论、排查、对话中，只要发现、讨论到、或定位出一个「错误」——无论是否已修复、无论大小——都必须登记到 `DEBUG_LOG.md`，不允许口头带过、当场改完就走。**

违反此律的常见代价（真实发生过）：
- 同一个坑被不同人/不同时间重复踩（如证书序列号前导零、进度条卡 100%、rg `-r` 当行号）。
- 误判根因后又推翻，时间浪费在反复横跳上。
- 问题被「修好了」但没留痕，后续回归时无法追溯「为什么改、怎么改、验证了没」。

## 二、什么算「错误」，必须记

只要满足以下任一条，就必须记：

1. **踩过的坑**：一个现象对应一个非显而易见的根因（不是「代码写错」这种一眼可见的语法错，而是「看似对、实际错的隐含约束」）。
2. **误判/走弯路**：包括「先判断是 A，后来发现是 B」的推理错误。这类记录极具价值——它本身就是坑位。
3. **跨来源/跨层不一致**：同一概念在两个来源格式不同（如证书序列号前导零）、阶段语义混淆（上传完成≠安装开始）、Swift↔Rust 边界不一致。
4. **违反项目约束的写法**：绕根因（`--no-verify`）、破坏「上传与安装同一隧道」不变量、错误码撞号、文案重复等。
5. **待验证的根因假设**：还没最终钉死、但已形成「高概率根因」的判断，也要记，标注「待真机验证」，避免下次从头猜。

## 三、记什么（固定五要素）

每条记录必须含：**现象 → 根因 → 修复 → 涉及文件 → 验证状态**。
- 现象：用一句话说清楚「用户/日志看到了什么」，别写内部黑话。
- 根因：为什么会这样，落到具体文件 + 行号 + 那条隐含约束。
- 修复：改了什么；若「已修」只写结论 + 提交/代码位置；若「未修」明确写「待 X」。
- 涉及文件：精确路径，方便日后 grep。
- 验证状态：如实写「已真机验证 / 已编译通过 / 待 Xcode 编译 / 待真机」。**没验证不准写已修复**。

## 四、怎么记

1. **新条目追加到 `DEBUG_LOG.md`「历史记录」顶部（最新在前）**，先写日期。
2. 若属于「反复出现的高频坑」，同时**提炼成一句**进 `DEBUG_LOG.md`「常犯坑位」节（动手前先看这一节）。
3. **本文件（纪律）不用频繁改**；纪律是固定的，添加/修订要走明确共识。

## 五、对 AI 协作的现场要求

- 每次会话结束前，凡讨论中出现过「错误/坑/误判/待验证根因」，必须：
  1. 把新条目追加进 `DEBUG_LOG.md`；
  2. 若涉及高频坑，同步更新「常犯坑位」；
  3. 在回复里明确告诉用户「已记入 DEBUG_LOG」，并给出条目标题。
- 用户说「记住/记下来」时，按三、四节落地，不得只回「好的我记住了」。

## 六、检查清单（动手/收尾各问一次）

- [ ] 这次讨论有没有出现错误、坑、误判、待验证根因？
- [ ] 有没有已经存在但这次又踩到、且 DEBUG_LOG 里其实已写过的高频坑？
- [ ] 记录是否含「现象/根因/修复/文件/验证状态」五要素？
- [ ] 验证状态是否如实（没真机验证就别写「已修复」）？