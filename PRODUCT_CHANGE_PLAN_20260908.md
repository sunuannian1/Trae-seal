# Seal 产品改动方案：更新公告与引流

> 状态：待评审　日期：2026-09-08　范围：更新公告弹窗、公众号入口、老带新裂变（二期）

## 一、现状与目标

Seal 目前唯一的「更新」能力是启动时的一段文字提醒，代码在 `Seal/Infrastructure/UpdateChecker.swift`。它从 GitHub 的 `releases/latest` 拉取标题和正文，用字符串相等判断版本是否一致，既没有分级，也不会下载或安装，而且仓库名还指向旧的 `dmjorb/Seal-Releases`。用户看到提醒后，仍要自己去 GitHub 手动下载 IPA，再走一遍签名安装。

由此带来两个问题：「更新」这个高频触点没有被利用起来，Seal 也缺少一个稳定的、用户可以验证真伪的官方入口，教程、公告、答疑都散落在各个群里。

这次改动的目标是把更新提醒做成分级可控的公告位，同时把公众号立为唯一官方入口，为后续签名服务、VIP 会员的转化留出位置。

## 二、更新公告弹窗的分级

公告不能一刀切，按「旧版不更新会不会坏掉」划分三档：

| 级别 | 触发条件 | 弹窗表现 |
| --- | --- | --- |
| 强制更新 | 证书或签名协议变更、认证逻辑改版、安全漏洞，旧版继续使用会失败 | 不可关闭，文案「此版本已停止支持，请立即更新」，并配合功能层阻断 |
| 建议更新 | 新功能、普通缺陷修复 | 「立即更新」+「稍后」，稍后仍可继续使用 |
| 静默 | 文案、小改动 | 不弹窗，仅在「关于 · 检查更新」里可见 |

iOS 自签 App 没有 App Store，做不出系统级的强制更新。「强制」是壳内软强制：弹窗不可关闭，再让旧版在功能上无法继续，而不是系统级拦截。实现时要把这个边界写进代码注释，避免被误当成「真强制」。

分级怎么让 App 知道，有两种做法：

- 在仓库放一个 `update.json`，含 `latestVersion`、`minSupportedVersion`、`forceUpgradeBelow`、`changelog`、`announcement`。临时把某个版本提为强制时改这个文件即可，不用重发 IPA。这是推荐做法，因为「强制」判断经常需要事后追加。
- 在 Release 正文里用固定前缀标记级别，App 解析正文。省一个文件，但改级别要重新发版。

推荐前者。`UpdateChecker` 现在只认 `releases/latest`，改动后要同时读 `update.json` 和 Release 资产。

## 三、公众号作为入口

入口分两层：持久入口和弹窗软引导。

持久入口放在「我的 / 关于」页，做一个不打断操作的「关注官方」卡片：公众号名、二维码、一句说明。用户想找教程、反馈、验证真伪时随时能找到，这比每次弹窗催关注更符合技术用户的习惯，长期转化也更高。

弹窗软引导只占底部一行，形如「教程 · 交流 → 公众号『xxx』」，点了跳到二维码或公众号文章链接。主操作永远是「立即更新 / 稍后」，软引导不能抢层级。

iOS 无法一键让人「关注」公众号，只能给二维码图片（长按保存后用微信扫）或公众号文章链接（跳浏览器），所以入口本质是软的，设计上不做「不关注就不能用」。

公众号承接的落地动作：关注后自动回复「教程」给图文合集、「交流」给群入口。它的价值不在当下变现，而在提供一个可验证的官方身份，让签名服务、VIP 这类付费决策有信任锚点。

## 四、老带新裂变（二期）

老带新用的是 Seal 自己本就有边际成本的东西作为奖励，而不是拿用户体验换第三方佣金。

机制：老用户生成专属邀请码，新用户安装 Seal 并用该码完成首次签名，双方各得一次签名服务额度或 VIP 会员天数。额度、防刷（同设备、同 Apple ID 去重）在进入二期前细化。

第一阶段先不做裂变，集中把公告分级和公众号入口做稳。裂变要等签名服务或 VIP 有可发放的额度后再上，否则奖励没有承接对象。

## 五、涉及的代码改动

按依赖顺序排列：

1. `UpdateChecker.swift`：仓库名从 `dmjorb/Seal-Releases` 改为 `sunuannian1/Trae-seal`；版本比较换成语义化（处理 `v` 前缀、多段版本号），不再用字符串相等；返回结构从纯文字升级为带级别的公告模型。
2. 新增 `update.json` 的解析与字段定义，作为分级和 changelog 的来源。
3. 公告弹窗组件：把现有 `Alert` 换成自定义 View，支持三级展示和「立即更新 / 稍后」双按钮。
4. 「我的 / 关于」页新增「关注官方」卡片（二维码 + 公众号名）。
5. CI 发版同步：构建产物自动挂到 Release 资产，并在发版时同步更新 `update.json`。

下载、导入、重签、覆盖安装的完整闭环是后续独立一环，本方案先不展开，它依赖第 1、2 项就位。

## 六、明确不做

- 网盘拉新不作为主下载路径，最多留一个可选的「备用下载」通道。
- 系统通知暂不接入，自签 App 的后台通知不可靠，先只用启动时弹一次。
- 不追求真强制更新，iOS 自签场景做不到，只做壳内软强制。

## 七、分期

- 第一期：公告分级、`update.json`、语义化版本比较、公众号持久入口。
- 第二期：下载-导入-重签-覆盖安装闭环、弹窗软引导。
- 第三期：老带新裂变，依赖签名服务或 VIP 额度落地。